/**
 * This takes sum of the 2 numbers given
 * @param {number} a input any number 
 * @param {number} b that number plus another number
 * @returns {number} the sum of variables a and b
 */
export default function file(a, b) {
    
    return a + b;
    
  }
  
